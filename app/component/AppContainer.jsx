"use client"

import { useEffect , useRef , useState } from "react"


const StyledContainer = ({children}) => {

const bodyRef = useRef(body.current)



  return (
    <div>{children}</div>
  )
}

export default StyledContainer